# Shared checkpoint lifecycle.  `last.pt` is the resumable checkpoint and
# `latest.pt` is its local alias; named checkpoints remain case-run artifacts.
checkpointing:
  enabled: true
  every_epochs: 5
  save_epoch_one: true
